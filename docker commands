docker exec <container name> ls - execute command and return result into terminal itself.

docker exec -it <container name> bash - to attach for teminal do not disconnect

docker network create -d bridge youtube -- to create a bridge network

docker network ls -- to list out all networks

docker run -it --network=youtube --name freddy ubuntu -- to create a custom network with image


dokcer run -it -v /users/home/test-folder <host machine pacth>:/home/abc <container path> ubuntu
